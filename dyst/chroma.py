"""chroma — DEPRECATED, no longer used.

The runtime chroma-key (green/blue-screen removal) feature was REMOVED from
DYST: green-screen assets are now expected to be pre-keyed (real alpha or
already-keyed media). This stub remains only so old imports don't crash.

The full implementation is preserved in git history at commit a36cc21
("Phase 3: chroma key (green/blue screen removal) + lazy per-frame mask
cache") — `git show a36cc21:dyst/chroma.py` to restore it.
"""
