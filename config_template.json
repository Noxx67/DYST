{
  "_comment": "DYST PER-FILE sidecar template. Copy this file next to a media file and rename it to match the media's base name (e.g. media/videos/scare.mp4 -> media/videos/scare.json), then edit the values you want. A .txt form is also supported (one `key=value` per line). Only the keys you set override the global config.json values; keys you leave out fall back to config.json (or its defaults). Delete a key to stop overriding it. The _comment/_example_sidecar/_hints/_hints_deprecated blocks are ignored by the app — they are documentation only. All per-file keys and how each interacts with the global config.json key are listed in _hints below.",
  "_example_sidecar": {
    "mode": "custom",
    "duration": 2.5,
    "volume": 1.5,
    "chroma": true,
    "chroma_key": "blue",
    "position_x": "-0.5~1.5",
    "scale": "0.5~1.5"
  },
  "image_display_seconds": 2,
  "duration": 2,
  "end_on_audio_end": false,
  "fade_in_seconds": 0,
  "fade_out_seconds": 0.2,
  "max_duration": 0,
  "speed": 1,
  "pitch": 1,
  "speed_pitch": 0,
  "max_playback_height": 480,
  "max_playback_fps": 30,
  "volume": 1,
  "mode": "fit",
  "opacity": 1,
  "position_x": 0.5,
  "position_y": 0.5,
  "scale_x": 1,
  "scale_y": 1,
  "scale": 1,
  "flip_h": false,
  "flip_v": false,
  "rotation": 0,
  "weight": 1,
  "chroma_hue_range": [35, 85],
  "chroma_saturation_range": [40, 255],
  "chroma_value_range": [40, 255],
  "_hints": {
    "image_display_seconds": "How long THIS image/animation stays up (seconds). Overrides the global `image_display_seconds` and takes PRIORITY over the per-file `duration` alias when both are present. Ignored by videos (they run to their end unless `max_duration` caps them).",
    "duration": "Per-file alias for `image_display_seconds`: how long THIS image/GIF stays up (seconds). Video ignores it. Interacts with `image_display_seconds` (that key wins if both are set), `end_on_audio_end` (the timer is ignored when that is true and a sidecar audio exists) and `max_duration` (the hard cap always wins).",
    "end_on_audio_end": "IMAGES/GIFs only: true = THIS image IGNORES its display time and stays up until its sidecar audio ENDS, then fades out. No sidecar audio = no effect (normal timer runs). Overrides the global `end_on_audio_end`. `max_duration` still applies and wins over a long audio track.",
    "fade_in_seconds": "Fade-in (seconds) for THIS image/GIF before the display clock starts: opacity 0 -> `opacity`. Videos ignore it. Overrides the global `fade_in_seconds`. Lifetime = fade_in + display + fade_out. `speed` divides all three.",
    "fade_out_seconds": "Fade-out (seconds) for THIS image/GIF. Videos end instantly (no fade). Overrides the global `fade_out_seconds`. The old name `fade_seconds` is still accepted as a deprecated alias. 0 = instant.",
    "max_duration": "Hard cap (seconds) for THIS media AND its audio. When it fires, the visual + audio stop immediately, with NO fade-out, and the overlay closes. Overrides the global `max_duration`; a per-file 0 explicitly DISABLES a global cap for this file. 0 = no cap. Supports ranges: \"1.0~5.0\".",
    "speed": "Playback speed multiplier for THIS media (> 0): scales video frame rate + own audio, GIF animation, sidecar audio, and image display time + fades (all timings / speed). Overrides the global `speed`, but is OVERRIDDEN by `speed_pitch` when that is > 0 (per-file > global). Default 1. Supports ranges: \"1.0~2.0\".",
    "pitch": "Audio pitch multiplier for THIS media (> 0), independent of speed (the audio is re-encoded via ffmpeg; needs ffmpeg when != 1). Overrides the global `pitch`, but is OVERRIDDEN by `speed_pitch` when that is > 0. Default 1. Supports ranges: \"0.5~1.5\".",
    "speed_pitch": "Combined speed+pitch for THIS media (>= 0): when > 0 it sets BOTH `speed` and `pitch` to this value and OVERRIDES the individual `speed`/`pitch` keys (per-file AND global). 0 = off (use speed/pitch separately). Per-file value wins over the global `speed_pitch`. Default 0. Supports ranges: \"1.0~2.0\".",
    "max_playback_height": "Performance cap (px) for THIS file: taller sources are downscaled (aspect kept) BEFORE decode/chroma-key/copy/paint. Videos + GIFs; still images ignore it. Overrides the global `max_playback_height`; 0 = no cap. Changing it rebuilds that file's precache.",
    "max_playback_fps": "Framerate cap for THIS video: frames above it are sampled (duration and audio untouched). GIFs ignore it. Overrides the global `max_playback_fps`; 0 = no cap. Changing it rebuilds that file's precache.",
    "volume": "Volume multiplier for THIS media (0.0-5.0). MULTIPLIES the global `volume`: final = global * per-file, so 0.5 halves it and 2 doubles it (per-file does NOT replace global). Ignored when the file has no audio. Default 1.",
    "mode": "How THIS media covers the screen: fit (default, whole media visible, aspect kept) | stretch (squish to screen) | cover-height (fit screen width, crop top/bottom) | cover-width (fit screen height, crop left/right) | custom (use the custom-mode keys below). Overrides the global `mode`. The removed 'cover' value falls back to 'fit'.",
    "opacity": "Overlay transparency for THIS media (0.0-1.0; 1 = fully opaque). Overrides the global `opacity`. Fades compose on top (fade-in 0 -> opacity, fade-out opacity -> 0).",
    "position_x": "Custom mode only: horizontal placement for THIS media, clamped to -1..2 (0 = left edge at the screen's left edge, 1 = right edge at the right edge, 0.5 = centered; outside 0..1 peeks/crops off-screen). Overrides the global `position_x`. Ignored unless `mode` is custom. Supports ranges: \"-0.5~1.5\".",
    "position_y": "Custom mode only: vertical placement for THIS media, clamped to -1..2 (0 = top edge at the screen's top edge, 1 = bottom edge at the bottom edge, 0.5 = centered; outside 0..1 peeks/crops off-screen). Overrides the global `position_y`. Ignored unless `mode` is custom. Supports ranges: \"-0.5~1.5\".",
    "scale_x": "Custom mode only: width multiplier for THIS media relative to the aspect-preserving 'fit' size (1 = whole media visible). Overrides the global `scale_x`. IMPORTANT: giving BOTH per-file `scale_x` AND `scale_y` OVERWRITES the per-file `scale`; a lone `scale_x`/`scale_y` overrides only its own axis. Ignored unless `mode` is custom. Supports ranges: \"0.5~1.5\".",
    "scale_y": "Custom mode only: height multiplier for THIS media relative to the 'fit' size. Overrides the global `scale_y`. See `scale_x` for how it interacts with `scale` / `scale_x`. Ignored unless `mode` is custom. Supports ranges: \"0.5~1.5\".",
    "scale": "Custom mode only (per-file only; there is no global `scale`): a uniform scale for THIS media that sets BOTH axes to the same value (e.g. 2 = twice as wide AND twice as tall). OVERWRITTEN when BOTH `scale_x` AND `scale_y` are given in the same sidecar; a lone `scale_x`/`scale_y` overrides only its own axis. A range (\"0.5~1.5\") draws ONE random value shared by both axes, so X and Y always match. Ignored unless `mode` is custom.",
    "flip_h": "Custom mode only: mirror THIS media horizontally (true/false; \"random\" picks a random bool each trigger). Overrides the global `flip_h`. Ignored unless `mode` is custom.",
    "flip_v": "Custom mode only: mirror THIS media vertically (true/false; \"random\" picks a random bool each trigger). Overrides the global `flip_v`. Ignored unless `mode` is custom.",
    "rotation": "Custom mode only: rotation in degrees for THIS media, around its own center (any number). Overrides the global `rotation`. Ignored unless `mode` is custom. Supports ranges: \"0~360\".",
    "weight": "Pick likelihood relative to all other media (>= 0, floats allowed; default 1): chance = weight / total_weight. 2 = twice as likely as a weight-1 file, 0.5 = half, 0 = NEVER picked (a warning is logged at scan). Works in ANY mode. Not shown in the top-level keys above only because it defaults to 1; add it to a sidecar to change it.",
    "chroma": "Per-file ON/OFF gate for chroma-key removal: true = filter THIS file using the global (or per-file) preset, false = SKIP filtering even when the global `chroma_key` is on (use it for assets that already have real alpha, or normal videos). `false` ALWAYS wins over a preset. Works in ANY mode. Omitted from the top-level keys above so copying this template does not force keying on; add it to a sidecar when needed.",
    "chroma_key": "Per-file chroma PRESET for THIS file: 'green' | 'blue' | 'weak green' | 'strong green' | 'weak blue' | 'strong blue' | 'custom'. Uses its own key independent of the global `chroma_key`. NOTE: setting a per-file preset turns keying ON for that file (it sets enabled = true), so only add it to files that really are shot against that colour screen; a per-file `chroma: false` still wins and skips keying. The precache key includes the preset, so changing it rebuilds the cache. Omitted from the top-level keys above so copying this template does not force keying on; see _example_sidecar. 'custom' ignores the presets: it takes the hue/saturation/value ranges from the per-file `chroma_hue_range` / `chroma_saturation_range` / `chroma_value_range` keys below instead.",
    "chroma_hue_range": "Per-file custom-chroma range [lo, hi] — OpenCV hue 0..179 (green ~60, blue ~120). Used ONLY when `chroma_key` is 'custom' (or the expert dict form uses preset 'custom'); ignored for every other preset. Default [35, 85].",
    "chroma_saturation_range": "Per-file custom-chroma range [lo, hi], saturation 0..255. Used ONLY when `chroma_key` is 'custom'; ignored otherwise. Default [40, 255].",
    "chroma_value_range": "Per-file custom-chroma range [lo, hi], value 0..255. Used ONLY when `chroma_key` is 'custom'; ignored otherwise. Default [40, 255].",
  },
  "_hints_deprecated": "OLD/REMOVED keys: 'fade_seconds' renamed to 'fade_out_seconds' (old name still accepted as a deprecated alias); the global 'cover' mode was removed (falls back to 'fit'); per-file 'chroma' is now a true/false ON/OFF gate, while 'chroma_key' is the per-file PRESET string."
}